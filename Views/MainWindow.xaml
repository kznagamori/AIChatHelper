<Window x:Class="AIChatHelper.Views.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:conv="clr-namespace:AIChatHelper.Core.Helper"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:controls="clr-namespace:AIChatHelper.Core.Controls"
        xmlns:models="clr-namespace:AIChatHelper.Models"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        mc:Ignorable="d"
        Title="AIChatHelper" Height="800" Width="1600" MinWidth="1440" MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Icon="pack://siteoforigin:,,,/Assets/app.ico">
    <Window.Resources>
        <conv:HistoryItemDisplayConverter x:Key="HistoryItemDisplayConverter" />
        <conv:ThemeAwareImageConverter x:Key="ThemeAwareImageConverter" />

        <Style x:Key="PlusMinusTreeViewItemStyle"
               TargetType="{x:Type TreeViewItem}">
            <Setter Property="IsExpanded" Value="False" />
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <StackPanel>
                            <Grid MinHeight="24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ToggleButton x:Name="Expander"
                                              Grid.Column="0"
                                              Width="18"
                                              Height="18"
                                              Margin="0,0,2,0"
                                              Padding="0"
                                              Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                    <TextBlock x:Name="ExpanderText"
                                               Text="+"
                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </ToggleButton>

                                <Border x:Name="HeaderBorder"
                                        Grid.Column="1"
                                        Padding="3,1"
                                        Background="Transparent"
                                        TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ContentPresenter x:Name="PART_Header"
                                                      ContentSource="Header"
                                                      TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"
                                                      HorizontalAlignment="Stretch" />
                                </Border>
                            </Grid>

                            <ItemsPresenter x:Name="ItemsHost"
                                            Margin="22,0,0,0" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpanderText" Property="Text" Value="-" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary}" />
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary.Foreground}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <HierarchicalDataTemplate DataType="{x:Type models:TemplateTreeNode}"
                                  ItemsSource="{Binding Children}"
                                  ItemContainerStyle="{StaticResource PlusMinusTreeViewItemStyle}">
            <TextBlock Text="{Binding Name}"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TreeViewItem}}"
                       TextTrimming="CharacterEllipsis"
                       ToolTip="{Binding FullPath}" />
        </HierarchicalDataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding LeftColumnWidth, Mode=TwoWay}" />
            <ColumnDefinition Width="{Binding SplitterWidth, Mode=TwoWay}" />
            <ColumnDefinition Width="{Binding RightColumnWidth, Mode=TwoWay}" />
        </Grid.ColumnDefinitions>

        <!-- 左：チャットサイト追加コントロール + TabControl -->
        <Grid x:Name="LeftPane"
              Grid.Column="0"
              Visibility="{Binding LeftPaneVisibility}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Padding="8,6"
                    BorderThickness="0,0,0,1"
                    BorderBrush="{DynamicResource MaterialDesign.Brush.Divider}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ComboBox x:Name="ChatSiteComboBox"
                              Grid.Column="0"
                              DisplayMemberPath="Name"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              materialDesign:HintAssist.Hint="追加するチャット"
                              SelectionChanged="ChatSiteComboBox_SelectionChanged"
                              Margin="0,0,6,0"
                              MinWidth="160" />

                    <Button x:Name="AddChatTabButton"
                            Grid.Column="1"
                            Width="40"
                            Height="40"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="選択したチャットサイトのタブを追加"
                            Click="AddChatTabButton_Click"
                            Margin="0,0,6,0"
                            IsEnabled="False">
                        <materialDesign:PackIcon Kind="Plus" Width="20" Height="20" />
                    </Button>

                    <Button x:Name="ToggleTabDisplayModeButton"
                            Grid.Column="2"
                            Width="116"
                            Height="40"
                            Padding="10,0"
                            BorderThickness="1"
                            Focusable="False"
                            Cursor="Hand"
                            ToolTip="現在: タブ表示。クリックすると縦分割に切り替えます"
                            AutomationProperties.Name="現在: タブ表示。クリックすると縦分割に切り替えます"
                            Click="ToggleTabDisplayModeButton_Click">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <materialDesign:PackIcon x:Name="ToggleTabDisplayModeIcon"
                                                     Kind="Tab"
                                                     Width="20"
                                                     Height="20"
                                                     Margin="0,0,4,0" />
                            <TextBlock x:Name="ToggleTabDisplayModeText"
                                       Text="タブ表示"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Grid x:Name="LeftPaneDisplayHost"
                  Grid.Row="1">
                <TabControl x:Name="TabControlMain"
                            SelectionChanged="TabControlMain_SelectionChanged">
                    <TabControl.Template>
                        <ControlTemplate TargetType="TabControl">
                            <DockPanel>
                                <ScrollViewer DockPanel.Dock="Top"
                                            HorizontalScrollBarVisibility="Disabled"
                                            VerticalScrollBarVisibility="Auto">
                                    <WrapPanel x:Name="HeaderPanel"
                                            IsItemsHost="True"
                                            Orientation="Horizontal"
                                            KeyboardNavigation.TabIndex="1"/>
                                </ScrollViewer>
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                Content="{TemplateBinding SelectedContent}"
                                                ContentSource="SelectedContent"
                                                KeyboardNavigation.TabIndex="2"/>
                            </DockPanel>
                        </ControlTemplate>
                    </TabControl.Template>
                    <TabControl.ItemContainerStyle>
                        <Style TargetType="TabItem" BasedOn="{StaticResource MaterialDesignTabItem}">
                            <Setter Property="MaxWidth" Value="220" />
                            <Setter Property="Height" Value="40" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="FontWeight" Value="Normal" />
                        </Style>
                    </TabControl.ItemContainerStyle>
                </TabControl>

                <Grid x:Name="SplitTabContentHost"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ClipToBounds="True"
                      Visibility="Collapsed" />
            </Grid>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1"
                    Width="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Stretch"
                    ShowsPreview="True"
                    Visibility="{Binding SplitterVisibility}"
                    DragCompleted="GridSplitter_DragCompleted" />

        <!-- 右：UI -->
        <Grid Grid.Column="2"
              Margin="10"
              Visibility="{Binding RightPaneVisibility}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 1行目：共通操作 -->
            <DockPanel Grid.Row="0" LastChildFill="False">
                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal"
                            Margin="0,0,8,0">
                    <Button Width="24" Height="24"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Click="OpenSettingsButton_Click"
                            Margin="0,0,8,0"
                            ToolTip="設定を開く"
                            x:Name="SettingsButton">
                            <materialDesign:PackIcon
                                Width="24"
                                Height="24"
                                Kind="Cog" />
                    </Button>
                    <Button Width="24" Height="24"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{Binding OpenInformationWindowCommand}"
                            Margin="0,0,8,0"
                            x:Name="InformationButton">
                            <materialDesign:PackIcon
                                Width="24"
                                Height="24"
                                Kind="InformationOutline" />
                    </Button>
                </StackPanel>
            </DockPanel>

            <!-- 2行目：履歴・テンプレートタブ -->
            <TabControl x:Name="RightPaneContentTabControl"
                        Grid.Row="1"
                        Margin="0,8,0,8"
                        Height="370"
                        SelectedValuePath="Tag"
                        SelectedValue="{Binding RightPaneSelectedTab, Mode=TwoWay}">
                <TabItem Header="履歴"
                         Tag="History">
                    <Grid Margin="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <materialDesign:Card Grid.Row="0"
                                             Padding="8,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ComboBox Grid.Column="0"
                                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                          materialDesign:HintAssist.Hint="検索"
                                          ItemsSource="{Binding SearchHistories}"
                                          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                          IsEditable="True"
                                          Width="Auto"
                                          HorizontalAlignment="Stretch">
                                    <ComboBox.InputBindings>
                                        <KeyBinding Key="Return" Command="{Binding SearchHistoryCommand}" />
                                    </ComboBox.InputBindings>
                                </ComboBox>

                                <Button Grid.Column="1"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        Command="{Binding ClearSearchCommand}"
                                        Margin="4,0,0,0"
                                        ToolTip="検索クリア">
                                    <materialDesign:PackIcon Kind="Close" Width="18" Height="18" />
                                </Button>

                                <Button Grid.Column="2"
                                        Width="40"
                                        Height="40"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        Command="{Binding OpenHistoryManagementWindowCommand}"
                                        Margin="4,0,0,0"
                                        ToolTip="履歴管理"
                                        x:Name="HistoryButton">
                                    <materialDesign:PackIcon Width="20"
                                                             Height="20"
                                                             Kind="History" />
                                </Button>
                            </Grid>
                        </materialDesign:Card>

                        <Border Grid.Row="1"
                                Margin="0,8,0,0"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource MaterialDesign.Brush.Divider}"
                                CornerRadius="4">
                            <controls:SlackStyleHistoryView x:Name="SlackHistoryView"
                                                            HistoryItems="{Binding HistoryItems}"
                                                            SelectedHistoryItem="{Binding SelectedHistoryItem, Mode=TwoWay}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="HistoryItemDoubleClicked">
                                        <i:InvokeCommandAction
                                            Command="{Binding HistoryClickedCommand}"
                                            CommandParameter="{Binding SelectedHistoryItem}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </controls:SlackStyleHistoryView>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="テンプレート"
                         Tag="Template">
                    <Grid Margin="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <ProgressBar Grid.Row="0"
                                     Height="4"
                                     IsIndeterminate="True">
                            <ProgressBar.Style>
                                <Style TargetType="ProgressBar">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTemplateTreeLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ProgressBar.Style>
                        </ProgressBar>

                        <TextBlock Grid.Row="1"
                                   Text="{Binding TemplateLoadErrorMessage}"
                                   Margin="0,4,0,8"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource MaterialDesign.Brush.ValidationError}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TemplateLoadErrorMessage}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Grid.Row="2"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource MaterialDesign.Brush.Divider}"
                                CornerRadius="4">
                            <TreeView x:Name="TemplateTreeView"
                                      ItemsSource="{Binding TemplateTreeNodes}"
                                      Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                      ItemContainerStyle="{StaticResource PlusMinusTreeViewItemStyle}"
                                      SelectedItemChanged="TemplateTreeView_SelectedItemChanged"
                                      MouseDoubleClick="TemplateTreeView_MouseDoubleClick"
                                      KeyDown="TemplateTreeView_KeyDown" />
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- 3行目：TextBox -->
            <Grid Grid.Row="2" x:Name="EditorContainer">
                <controls:AvalonTextEditor x:Name="AvalonEditor"
                                        Text="{Binding EditorText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="ExecuteCommand">
                            <i:InvokeCommandAction
                                Command="{Binding ChatToServiceCommand}"
                                CommandParameter="{Binding ElementName=RootWindow, Path=ActiveChatCommandTarget}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </controls:AvalonTextEditor>
            </Grid>

            <!-- 4行目：テーマ、送信設定、コマンドボタンを1段で配置する。 -->
            <Grid Grid.Row="3" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- テーマ固定と Windows 追従を、名称が分かる1つのメニューから選択する。 -->
                <materialDesign:PopupBox x:Name="ThemeMenuButton"
                                         Grid.Column="0"
                                         MinWidth="96"
                                         Height="40"
                                         ToolTip="テーマ"
                                         AutomationProperties.Name="テーマを選択"
                                         PlacementMode="TopAndAlignLeftEdges"
                                         StaysOpen="False">
                    <materialDesign:PopupBox.ToggleContent>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="ThemeLightDark"
                                                     Width="22"
                                                     Height="22"
                                                     VerticalAlignment="Center" />
                            <TextBlock Text="テーマ"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       TextWrapping="NoWrap" />
                        </StackPanel>
                    </materialDesign:PopupBox.ToggleContent>
                    <materialDesign:PopupBox.PopupContent>
                        <!-- 固定上限を設けず、長いテーマ名や文字拡大時は内容に合わせて広げる。 -->
                        <StackPanel MinWidth="300">
                            <MenuItem Header="Windows の設定に合わせる"
                                      IsCheckable="True"
                                      IsChecked="{Binding IsSystemThemeMode, Mode=OneWay}"
                                      Command="{Binding SelectThemeModeCommand}"
                                      CommandParameter="{x:Static models:ThemeMode.System}"
                                      Click="ThemeMenuItem_Click" />
                            <MenuItem Header="ライト"
                                      IsCheckable="True"
                                      IsChecked="{Binding IsLightThemeMode, Mode=OneWay}"
                                      Command="{Binding SelectThemeModeCommand}"
                                      CommandParameter="{x:Static models:ThemeMode.Light}"
                                      Click="ThemeMenuItem_Click" />
                            <MenuItem Header="ダーク"
                                      IsCheckable="True"
                                      IsChecked="{Binding IsDarkThemeMode, Mode=OneWay}"
                                      Command="{Binding SelectThemeModeCommand}"
                                      CommandParameter="{x:Static models:ThemeMode.Dark}"
                                      Click="ThemeMenuItem_Click" />
                        </StackPanel>
                    </materialDesign:PopupBox.PopupContent>
                </materialDesign:PopupBox>

                <!-- 送信設定と実行コマンドは同じ順序で右寄せ表示する。 -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,0,8,0">
                    <CheckBox Content="送信後実行"
                              IsChecked="{Binding ExecuteAfterSend, Mode=TwoWay}"
                              IsEnabled="{Binding IsSendingNot}"
                              VerticalAlignment="Center"
                              Margin="0,0,12,0"
                              ToolTip="チェック時は、入力後にサービス側の実行ボタンも押します" />
                    <Button Content="{Binding ChatButtonText}"
                            Width="120"
                            Command="{Binding ChatToServiceCommand}"
                            CommandParameter="{Binding ElementName=RootWindow, Path=ActiveChatCommandTarget}"
                            Margin="0,0,8,0"
                            Style="{StaticResource MaterialDesignPaperButton}"
                            IsEnabled="{Binding IsSendingNot}" />
                    <Button Content="クリア"
                            Width="120"
                            Command="{Binding ClearEditorCommand}"
                            Style="{StaticResource MaterialDesignPaperButton}"
                            Margin="0,0,8,0" />
                    <Button Content="保存"
                            Width="120"
                            Command="{Binding SaveHistoryCommand}"
                            Style="{StaticResource MaterialDesignPaperButton}"
                            Margin="0,0,8,0" />
                    <Button Content="ファイルに保存"
                            Width="120"
                            Command="{Binding SaveHistoryToFileCommand}"
                            Style="{StaticResource MaterialDesignPaperButton}"
                            Margin="0,0,8,0" />
                </StackPanel>
            </Grid>
        </Grid>

        <!-- 左ペインの右上に直角三角形を配置（右上に開いた形） -->
        <Canvas Grid.Column="0" Panel.ZIndex="999" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Path Fill="Yellow"
                Data="M 0,0 L 8,0 L 8,8 Z"
                Cursor="Hand"
                Canvas.Right="0"
                Canvas.Top="0">
                <Path.InputBindings>
                    <MouseBinding MouseAction="LeftClick"
                                Command="{Binding ShowLeftPaneOnlyCommand}" />
                </Path.InputBindings>
            </Path>
        </Canvas>

        <!-- 右ペインの右上に直角三角形を配置（右上に開いた形） -->
        <Canvas Grid.Column="2" Panel.ZIndex="999" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Path Fill="Yellow"
                Data="M 0,0 L 8,0 L 8,8 Z"
                Cursor="Hand"
                Canvas.Right="0"
                Canvas.Top="0">
                <Path.InputBindings>
                    <MouseBinding MouseAction="LeftClick"
                                Command="{Binding ShowRightPaneOnlyCommand}" />
                </Path.InputBindings>
            </Path>
        </Canvas>
    </Grid>
</Window>
